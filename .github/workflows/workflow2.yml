name: Base2 workflow

on:
  workflow_dispatch:
    inputs:
      run1:
        required: false
        default: 'ON'
      run2:
        required: false
        default: 'ON'

jobs:
  good-bye:
    name: Good Bye
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Echo Good Bye
        run: echo 'Good Bye'
        shell: bash

      - name: Execute Run1
        if: ${{ github.event.inputs.run1 == 'ON' }}
        run: echo 'Run1 !'
        shell: bash

      - name: Execute Run2
        if: ${{ github.event.inputs.run2 == 'ON' }}
        run: echo 'Run2 !'
        shell: bash
